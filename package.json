{
  "name": "stellarsettle-client",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Responsive web client for StellarSettle.",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node scripts/preview.js"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.9.3"
  },
  "engines": {
    "node": ">=20"
  }
}
