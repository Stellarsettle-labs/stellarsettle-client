# StellarSettle Client

Invoice, payment link, and settlement rails for Stellar USDC merchants and freelancers.

## Run

```powershell
npm install
npm run build
npm start
```

The preview server listens on `http://localhost:5173`.
